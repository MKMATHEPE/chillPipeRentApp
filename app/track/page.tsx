'use client';
import { useEffect, useState } from 'react';
import { Check, Clock3, CreditCard, PackageCheck, RotateCcw, Search, Truck } from 'lucide-react';
import AppDock from '../components/AppDock';
type Booking={reference:string;status:string;total:number;deposit:number;deliveryFee:number|null;phone:string;customer:{name:string;date:string;location:string};quantities:Record<string,number>};
const money=(n:number)=>`R${n.toLocaleString('en-ZA')}`;const rank:Record<string,number>={awaiting_review:0,approved:1,payment_review:1,paid:2,handed_over:3,returned:4,complete:5};
export default function Track(){const [booking,setBooking]=useState<Booking|null>(null);const [reference,setReference]=useState('');const [phone,setPhone]=useState('');const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function load(ref=reference,tel=phone){if(!ref||!tel)return;setLoading(true);setError('');try{const response=await fetch(`/api/bookings?reference=${encodeURIComponent(ref)}&phone=${encodeURIComponent(tel)}`,{cache:'no-store'});const data=await response.json();if(!response.ok)throw new Error(data.error||'Booking not found.');setBooking(data);setReference(ref);setPhone(tel);localStorage.setItem('chill-pipe-booking-access',JSON.stringify({reference:ref,phone:tel}));}catch(e){setError(e instanceof Error?e.message:'Booking not found.')}finally{setLoading(false)}}useEffect(()=>{const params=new URLSearchParams(window.location.search);const raw=localStorage.getItem('chill-pipe-booking-access');const saved=raw?JSON.parse(raw):{};const ref=params.get('reference')||saved.reference||'';const tel=params.get('phone')||saved.phone||'';setReference(ref);setPhone(tel);if(ref&&tel)void load(ref,tel);},[]);if(!booking)return <main className="track-lookup"><header className="site-header"><a className="brand" href="/"><img src="/chill-pipe-logo.png" alt="The Chill Pipe"/></a></header><section><Search/><p className="eyebrow">Find your rental</p><h1>Track a booking</h1><p>Enter the reference and WhatsApp number used for the order.</p><label>Booking reference<input value={reference} onChange={e=>setReference(e.target.value.toUpperCase())} placeholder="CP-ABC123"/></label><label>WhatsApp number<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="071 234 5678"/></label>{error&&<p className="lookup-error" role="alert">{error}</p>}<button onClick={()=>void load()} disabled={loading||!reference||!phone}>{loading?'Looking up…':'Track booking'}</button></section><AppDock active="track"/></main>;const step=rank[booking.status]??0;const heading=booking.status==='approved'?'Your quote is ready.':booking.status==='payment_review'?'Payment received for review.':booking.status==='complete'?'Deposit refund approved.':step>=2?'Your rental is confirmed.':'We’re reviewing your request.';const rows=[['Request received','Your details were saved securely.',PackageCheck,0],['Quote approved','Availability and delivery pricing confirmed.',Clock3,1],['Payment verified','Your equipment is reserved.',CreditCard,2],['Handover','Delivery or collection is completed.',Truck,3],['Return & deposit','Inspection and deposit refund.',RotateCcw,5]] as const;return <main className="status-app"><header className="site-header"><a className="brand" href="/"><img src="/chill-pipe-logo.png" alt="The Chill Pipe"/></a><div className="header-note">Booking {booking.reference}</div></header><section className="status-hero"><p className="eyebrow">Rental status</p><h1>{heading}</h1><p>This status is shared across devices and updates when The Chill Pipe processes your booking.</p></section><section className="status-layout"><div className="status-timeline">{rows.map(([title,copy,Icon,needed])=><div className={step>=needed?'timeline-row active':'timeline-row'} key={title}><span>{step>=needed?<Check/>:<Icon/>}</span><div><strong>{title}</strong><p>{copy}</p></div></div>)}</div><aside className="status-card"><p className="eyebrow">Booking summary</p><div><span>Reference</span><strong>{booking.reference}</strong></div><div><span>Rental date</span><strong>{booking.customer.date}</strong></div><div><span>Rental</span><strong>{money(booking.total)}</strong></div><div><span>Refundable deposit</span><strong>{money(booking.deposit)}</strong></div><div><span>Delivery / collection</span><strong>{booking.deliveryFee===null?'Pending':money(booking.deliveryFee)}</strong></div><div className="status-total"><span>Total</span><strong>{money(booking.total+booking.deposit+(booking.deliveryFee||0))}</strong></div>{booking.status==='approved'&&<a className="status-cta" href={`/payment?reference=${encodeURIComponent(booking.reference)}&phone=${encodeURIComponent(phone)}`}>Continue to payment</a>}{booking.status==='awaiting_review'&&<span className="status-cta disabled">Awaiting approval</span>}{booking.status==='payment_review'&&<span className="status-cta disabled">Payment being verified</span>}<button className="status-refresh" onClick={()=>void load(booking.reference,phone)}>Refresh status</button></aside></section><AppDock active="track"/></main>}
